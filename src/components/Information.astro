---
const {
  title = "¿Desea más información?",
  phone = "+506 24375750",
  whatsapp = "+506 70717800",
  email = "sales@angular-tech.com",
  buttonText = "Evaluación gratuita"
} = Astro.props;
---

<section id="information" class="cta">
  <div class="cta__box">
    <h2>{title}</h2>

    <div class="cta__content">
      <span class="cta__line"></span>

      <div>
        <p><strong>Contáctenos.</strong></p>
        <p>Tel. Oficina: {phone}</p>
        <p>Whatsapp: {whatsapp}</p>
        <p>Correo: {email}</p>
      </div>
    </div>

    <button class="cta__btn">
      <span>+</span>
      {buttonText}
    </button>
  </div>
</section>

<style>
  .cta {
    border-top: #1f3b73 10px solid;
    background: white;
    padding: 4rem 1rem;
    display: flex;
    justify-content: center;
  }

  .cta__box {
    background: #f5f7fa;
    padding: 3rem;
    max-width: 700px;
    width: 100%;
    position: relative;
    padding-bottom: 5rem;
  }

  h2 {
    text-align: center;
    color: #1f3b73;
    margin-bottom: 2rem;
    font-size: 2rem;
  }

  p{
    color: #1f3b73;
    font-size: 1rem;
  }

  .cta__content {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
    justify-content: center;
  }

  .cta__line {
    width: 80px;
    height: 3px;
    background: #1f3b73;
    margin-top: 10px;
  }

  .cta__btn {
    margin-top: 1rem;
    background: red;
    color: white;
    border: none;
    padding: 0.6rem 1rem 0.6rem 0rem;
    cursor: pointer;
    margin-left: auto;
    display: block;
  }

  .cta__btn span {
    background: #1f3b73;
    color: white;
    padding: 0.6rem 1rem;
    margin-right: 0.5rem;
  }

  @media (max-width: 600px) {
    .cta__content {
      flex-direction: column;
    }

    .cta__btn {
      position: static;
      margin-top: 2rem;
      width: auto;
      margin-left: auto;
      margin-right: auto;
    }
  }
</style>